use crate::adder_v2::adder::Adder;

impl Adder {
    pub fn check_id_all_match(&self) {
        // // wires id all match
        // for (i, (id, _)) in 0..self.wires {
            
        // }
    }
}