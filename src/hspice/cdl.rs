use crate::{cell_parse::ProcessAndProject, std::adder::Adder};

impl Adder {
    pub fn to_cdl(&self, process : ProcessAndProject) -> String {
        let mut txt = String::new();

        txt
    }
}